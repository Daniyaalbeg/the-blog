---
export interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date ? date.toISOString() : new Date().toISOString()}>
	<!-- {
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	} -->
</time>
